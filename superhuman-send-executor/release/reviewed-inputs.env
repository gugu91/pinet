# Exact SHA-256 values must be set in a separately reviewed release commit.
# The release builder reads this file only from its private `git archive HEAD` snapshot.
node_sha256=UNPINNED
shm_sha256=UNPINNED
